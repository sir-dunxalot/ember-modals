{{#each modals.modalsInDom as |modal|}}
  {{#ember-modal-overlay}}
    {{#ember-modal modal=modal}}
      {{component modal.componentName
        targetObject=(if modal.context modal.context targetObject)
      }}
    {{/ember-modal}}
  {{/ember-modal-overlay}}
{{/each}}
